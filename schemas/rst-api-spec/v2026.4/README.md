# RST API Spec v2026.4 schemas

Place the official RST API Spec v2026.4 schema files in these folders:

- `json/` for JSON schemas (including `testCaseLog` schema)
- `xml/` for XML schemas used by protocol payload validation

The Pytest scaffold in `tests/` is wired to these paths.
