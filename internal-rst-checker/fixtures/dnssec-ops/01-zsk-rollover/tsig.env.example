# TSIG key used to authorise zone transfer for the ZSK rollover monitor.
# Replace `<set-locally-base64>` with the real value at runtime.
TSIG_NAME=rst-tsig-01
TSIG_ALGORITHM=hmac-sha256
TSIG_SECRET=<set-locally-base64>
