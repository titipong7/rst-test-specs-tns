# DNS suite local runtime template (placeholders only — never commit real values)
# Each value mirrors an `inc/dns/inputs.yaml` field. Replace with values
# appropriate for your test TLD before running the live checker.
DNS_TLD=example
DNS_GLUE_POLICY=narrow
DNS_PROBE_TIMEOUT_UDP_MS=1000
DNS_PROBE_TIMEOUT_TCP_MS=3000
